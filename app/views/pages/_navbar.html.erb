<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <%= link_to "BookStore", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse navbar-links" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <%= link_to "Livros", livros_path, class:"nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Contato", pages_contato_path, class:"nav-link" %>
                </li>
            </ul>

            <ul class="navbar-nav navbar-buttons">
                <% if user_signed_in? %>
                    <li class="nav-item">
                        <%= link_to "Editar Perfil", edit_user_registration_path, class:"btn btn-primary" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Sair", destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to "Cadastrar", new_user_registration_path, class:"btn btn-success" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Login", new_user_session_path, class:"btn btn-primary" %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>